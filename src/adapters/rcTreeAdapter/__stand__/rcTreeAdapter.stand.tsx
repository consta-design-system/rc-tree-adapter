import { createStand } from '##/stand/standConfig';

import image from './rcTreeAdapter.image.svg';

export default createStand({
  title: 'rcTreeAdapter',
  id: 'rcTreeAdapter',
  image,
  group: 'adapters',
  description: 'Адаптер для стилизации дерева.',
  version: '1.0.8',
  status: 'stable',
});
